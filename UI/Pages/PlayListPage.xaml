<Page
    x:Class="TewiMP.UI.Pages.PlayListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:TewiMP"
    xmlns:c="using:TewiMP.UI.Controls"
    xmlns:data="using:TewiMP.Core.Music"
    xmlns:dev="using:DevWinUI"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    Loaded="Page_Loaded" Unloaded="Page_Unloaded"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="BaseGridView"
                  SelectionMode="None"
                  ItemContainerStyle="{StaticResource NoPaddingListViewItemStyle}">
            <ListView.Header>
                <c:StickTitleHeader x:Name="StickTitleHeader" Title="播放列表">
                    <c:StickTitleHeader.CommandBar>
                        <CommandBar VerticalAlignment="Bottom" Margin="0,0,0,0"
                                    OverflowButtonVisibility="Auto" DefaultLabelPosition="Right">
                            <AppBarButton Label="刷新" AccessKey="R" Click="AppBarButton_Click">
                                <AppBarButton.Resources>
                                    <StaticResource x:Key="AppBarButtonBackground" ResourceKey="MusicAlbumAccentBrush" />
                                    <StaticResource x:Key="AppBarButtonBackgroundPointerOver" ResourceKey="MusicAlbumAccentBrushDark1" />
                                    <StaticResource x:Key="AppBarButtonBackgroundPressed" ResourceKey="MusicAlbumAccentBrushDark2" />
                                    <StaticResource x:Key="AppBarButtonForeground" ResourceKey="TextOnMusicAlbumAccentForegroundBrush" />
                                    <StaticResource x:Key="AppBarButtonForegroundPointerOver" ResourceKey="TextOnMusicAlbumAccentForegroundBrushDark1" />
                                    <StaticResource x:Key="AppBarButtonForegroundPressed" ResourceKey="TextOnMusicAlbumAccentForegroundBrushDark2" />
                                </AppBarButton.Resources>
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE72C;"/>
                                </AppBarButton.Icon>
                            </AppBarButton>
                            <AppBarButton Label="排序" AccessKey="L" Click="AppBarButton_Click_2">
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE8CB;"/>
                                </AppBarButton.Icon>
                            </AppBarButton>
                            <AppBarButton Label="添加/导入" AccessKey="A" Click="AppBarButton_Click_1">
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE710;"/>
                                </AppBarButton.Icon>
                            </AppBarButton>
                        </CommandBar>
                    </c:StickTitleHeader.CommandBar>
                </c:StickTitleHeader>
            </ListView.Header>
            <ItemsView x:Name="ItemsViewer" SelectionMode="None" Margin="16,0,16,44"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                <ItemsView.Resources>
                    <SolidColorBrush x:Key="ItemContainerPointerOverBackground" Color="Transparent"/>
                    <SolidColorBrush x:Key="ItemContainerPressedBackground" Color="Transparent"/>
                </ItemsView.Resources>
                <ItemsView.Layout>
                    <LinedFlowLayout ItemsStretch="Fill" LineHeight="320" LineSpacing="12" MinItemSpacing="12"/>
                </ItemsView.Layout>
                <ItemsView.ItemTemplate>
                    <DataTemplate x:DataType="data:MusicListData">
                        <ItemContainer>
                            <c:PlayListCard/>
                        </ItemContainer>
                    </DataTemplate>
                </ItemsView.ItemTemplate>
            </ItemsView>
        </ListView>
    </Grid>
</Page>
