<UserControl
    x:Class="TewiMP.Controls.LogItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TewiMP.Controls"
    xmlns:background="using:TewiMP.Background"
    xmlns:community="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContextChanged="UserControl_DataContextChanged"
    Loaded="UserControl_Loaded"
    Unloaded="UserControl_Unloaded"
    ActualThemeChanged="UserControl_ActualThemeChanged">
    <Grid>
        <community:SwitchPresenter x:Name="swp"
                                   TargetType="background:LogLevel"
                                   Value="Info">
            <community:Case Value="Info">
                <Rectangle Fill="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </community:Case>
            <community:Case Value="Warning">
                <Rectangle Fill="{ThemeResource SystemFillColorCautionBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity=".8"/>
            </community:Case>
            <community:Case Value="Error">
                <Rectangle Fill="{ThemeResource SystemFillColorCriticalBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity=".8"/>
            </community:Case>
        </community:SwitchPresenter>
        <TextBlock
            x:Name="t2" Grid.Row="1"
            IsTextSelectionEnabled="True"
            TextWrapping="WrapWholeWords"
            Padding="12,8">
            <Run x:Name="t1_1" FontWeight="SemiBold"/><Run x:Name="t1_2" FontWeight="SemiBold"/><Run x:Name="t2_1" FontWeight="Bold"/><Run x:Name="t2_2"/>
        </TextBlock>
    </Grid>
</UserControl>
